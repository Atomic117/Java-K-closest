Thomas Tran 

This directory contains:

The 3 various priority queues, respectively are: array-based, heap-based, imported
The KNN Class used to find KNN
PointSet used to instantize KNN and return the list containing the closest neighbours
LabelledPoint
A file containing:
	ALL console outputs for each version of Priority Queue for K (1, 10, 50, 100, 500, 2000)
	Running time Graph
	Discussion

DOES NOT CONTAIN THE DATA FILES (sift_query, sift_base etc)
